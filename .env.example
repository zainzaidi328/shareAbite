DATABASE_URL="file:./dev.db"
JWT_SECRET="sharebite-dev-secret-change-me-in-production-7f3a9b2c"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Optional: Cloudinary unsigned upload (falls back to URL input / local upload if unset)
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=""
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET=""

# Optional: SMTP for real emails (verification/reset links are logged to console if unset)
SMTP_HOST=""
SMTP_PORT=""
SMTP_USER=""
SMTP_PASS=""
EMAIL_FROM="ShareBite <no-reply@sharebite.app>"
